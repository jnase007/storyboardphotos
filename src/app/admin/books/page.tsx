import { BooksLibrary } from "@/components/admin/books-library";

export const metadata = { title: "Books Library | Storybook Photos Admin" };

export default function BooksPage() {
  return <BooksLibrary />;
}
